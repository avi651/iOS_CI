
workflows:
  spotify-workflow:
    name: Spotify Pipeline
    scripts:
      - name: iOS test
        script: |
          xcode-project run-tests \
            --workspace SportifyCI.xcworkspace \
            --scheme Sportify
    publishing:
     email: 
      recipients:
        - kumaravi651@gmail.com        